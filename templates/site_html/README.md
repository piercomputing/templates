# Plain HTML — Pier starter template

A plain HTML/CSS static site — no build step, no dependencies.

Edit `index.html` and deploy.

## Deploy on Pier

Instantly from the console — **Deploy → From a template** — or from this
directory with the CLI:

```bash
pier create site
```
