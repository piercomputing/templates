# Hugo — Pier starter template

A static site built with [Hugo](https://gohugo.io).

## Run locally

```bash
hugo server
```

## Build

```bash
hugo --minify   # static output in public/
```

## Deploy on Pier

Instantly from the console — **Deploy → From a template** — or from this
directory with the CLI:

```bash
pier create site
```
